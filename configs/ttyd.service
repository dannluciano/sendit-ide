[Unit]
Description=TTYD
After=syslog.target
After=network.target

[Service]
ExecStart=/usr/bin/ttyd docker run -it --rm --workdir="/root/" --cpus=1 --memory=256MB sendit-ide_vm 
Type=simple
Restart=always
User=root
Group=root

[Install]
WantedBy=multi-user.target