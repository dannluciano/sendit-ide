[Unit]
Description=TTYD
After=syslog.target
After=network.target

[Service]
ExecStart=/usr/bin/ttyd docker run -it --rm sendit-ide_vm
Type=simple
Restart=always
User=root
Group=root

[Install]
WantedBy=multi-user.target